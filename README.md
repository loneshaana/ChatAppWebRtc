# Chat App using WebRtc and WebSockets

## Available Scripts

In the project directory, you can run:

### `npm start`
    This will spin up the react UI

### `npm run start-server`
    This will spin up the nodejs backend